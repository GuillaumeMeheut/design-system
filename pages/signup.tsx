import type { NextPage } from 'next'
import { AppButton } from '../components'

const SignUp: NextPage = () => {
    return (
        <div>
            <AppButton text={`S'inscrire`}></AppButton>
        </div >
    )
}

export default SignUp
